*.o
*.obj
*.exe
*.res
*.map

# this project's own clone of claude-os2-toolkit: a separate repo with its own
# history, merged back to the toolkit rather than committed here
claude-os2-toolkit/

# runtime settings written by the app itself
np2.ini
